{
	"name": "arweave-app-extension",
	"description": "",
	"type": "module",
	"scripts": {
		"dev": "nodemon --ignore build --ignore archive --ext * build.js dev",
		"build": "node build.js chrome firefox",
		"deploy:firefox": "node build.js firefox && web-ext-submit --channel=listed --source-dir=build --artifacts-dir=archive"
	},
	"author": "",
	"devDependencies": {
		"@types/chrome": "^0.0.181",
		"@types/node": "^17.0.36",
		"archiver": "^5.3.1",
		"arweave-wallet-connector": "^0.0.30",
		"esbuild": "^0.14.25",
		"nodemon": "^2.0.16",
		"web-ext-submit": "^6.8.0"
	}
}
